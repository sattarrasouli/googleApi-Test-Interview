import { IEndpointTypes } from "../utils/interfaces";

/**
 * store every query in object using in api call.
 * ease of use and modification.
 */

export const ENDPOINT: IEndpointTypes = {
    books: "/books/v1/volumes",
}